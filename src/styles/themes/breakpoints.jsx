// ==============================|| DEFAULT THEME - BREAKPOINTS  ||============================== //

const Breakpoints = (xs, sm, md, lg, xl) => ({
    values: {
        xs,
        sm,
        md,
        lg,
        xl
    }
});

export default Breakpoints;
