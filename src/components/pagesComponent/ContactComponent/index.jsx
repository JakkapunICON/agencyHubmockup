import { useEffect, useState } from 'react';
import ApiServices from '../../../services/ApiServices';

function ContactComponent() {
    // const [data, setData] = useState([]);

    // const getData = async () => {
    //     await ApiServices.GetDataJson().then((response) => {
    //         setData(response.data.item.filter((x) => x.name === 'Agency_Hub_Mockupapi'));
    //     });
    // };

    // useEffect(() => {
    //     getData();
    // }, []);

    return (
        // <div>
        //     <ul>
        //         {data.map((item, i) => (
        //             <div key={i}>
        //                 <li>{item.name}</li>
        //                 <hr />
        //             </div>
        //         ))}
        //     </ul>
        // </div>
        <></>
    );
}

export default ContactComponent;
